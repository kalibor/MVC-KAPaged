@model TestPagedList.KAPagedTool.KAPaged<TestPagedList.Models.Bear>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@if(Model.Count()>0){
 
    foreach(var bear in Model)
    {
        <div>@bear.Name</div>
    }
}

頁面顯示數：@Model.PagedContext.PageSize
<br />
目前頁數：@Model.PagedContext.NowPage
<br />
最大頁數：@Model.PagedContext.MaxPage